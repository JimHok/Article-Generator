This folder stores raw training data.

Note:

- The default training flow uses a Hugging Face public dataset (`ag_news`) directly.
- Local self-built datasets are not used in the current training pipeline.
